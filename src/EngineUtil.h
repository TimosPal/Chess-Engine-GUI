#ifndef ENGINEUTIL_H
#define ENGINEUTIL_H

namespace ChessEngine {

    enum Color {
        White, Black
    };

    enum File{
        A, B, C, D, E, F, G, H
    };

    enum PieceType{
        Pawn, Rook, Bishop, Knight, King, Queen
    };

}

#endif
